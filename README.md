# 🏥 Medical Analysis API

Backend-сервис для автоматизированного сбора, распознавания и анализа медицинских документов с использованием OCR и AI.

## 🚀 Стек технологий

- **Python 3.11+** + **FastAPI**
- **PostgreSQL** + **SQLAlchemy 2.0** (async)
- **Alembic** — миграции БД
- **Tesseract OCR** + **OpenAI GPT-4** — распознавание и структурирование анализов
- **Redis** — кэширование
- **Docker** — контейнеризация

## 📁 Структура проекта

```
medical-analysis-api/
├── app/
│   ├── api/v1/             # API эндпоинты
│   │   ├── auth.py         # Авторизация (JWT)
│   │   ├── users.py        # Профиль пользователя
│   │   ├── analyses.py     # Загрузка и обработка анализов
│   │   ├── recommendations.py  # Рекомендации товаров
│   │   ├── calendar.py     # Календарь здоровья
│   │   └── products.py     # Каталог товаров
│   ├── core/               # Конфигурация
│   │   ├── config.py       # Настройки приложения
│   │   ├── security.py     # JWT, хеширование паролей
│   │   └── database.py     # Подключение к БД
│   ├── models/             # SQLAlchemy модели
│   │   ├── user.py         # Пользователи
│   │   ├── analysis.py     # Анализы и файлы
│   │   ├── biomarker.py    # Биомаркеры и референсы
│   │   ├── product.py      # Товары и рекомендации
│   │   └── reminder.py     # Напоминания
│   ├── schemas/            # Pydantic схемы (валидация)
│   ├── services/           # Бизнес-логика
│   │   ├── ocr.py          # OCR (Tesseract)
│   │   ├── ai_parser.py    # AI парсинг (GPT)
│   │   └── recommendations.py  # Генерация рекомендаций
│   └── main.py             # Точка входа FastAPI
├── alembic/                # Миграции БД
├── scripts/                # Утилиты
│   └── seed_biomarkers.py  # Заполнение справочника биомаркеров
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile
├── Makefile                # Команды для разработки
└── requirements.txt
```

## ⚡ Быстрый старт

### Вариант 1: Docker (рекомендуется)

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd medical-analysis-api

# 2. Создать .env файл
cp .env.example .env
# Заполнить OPENAI_API_KEY и SECRET_KEY

# 3. Запустить
docker-compose up -d --build

# 4. Применить миграции
docker-compose exec api alembic upgrade head

# 5. Заполнить справочник биомаркеров
docker-compose exec api python -m scripts.seed_biomarkers
```

API доступен на http://localhost:8000

### Вариант 2: Локально

```bash
# 1. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Установить Tesseract OCR
# macOS: brew install tesseract tesseract-lang
# Ubuntu: apt install tesseract-ocr tesseract-ocr-rus

# 4. Настроить переменные окружения
cp .env.example .env
# Заполнить DATABASE_URL, OPENAI_API_KEY, SECRET_KEY

# 5. Запустить PostgreSQL и применить миграции
alembic upgrade head

# 6. Заполнить справочник биомаркеров
python -m scripts.seed_biomarkers

# 7. Запустить сервер
make dev
# или: uvicorn app.main:app --reload --port 8000
```

## 📚 API Документация

После запуска доступна:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **OpenAPI JSON**: http://localhost:8000/openapi.json

### Основные эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/v1/auth/register` | Регистрация |
| POST | `/api/v1/auth/login` | Авторизация (получение JWT) |
| POST | `/api/v1/analyses/upload` | Загрузка анализа (PDF/фото) |
| GET | `/api/v1/analyses` | Список анализов |
| GET | `/api/v1/analyses/{id}` | Детали анализа с биомаркерами |
| GET | `/api/v1/recommendations` | Рекомендации товаров |
| POST | `/api/v1/calendar/reminders` | Создание напоминания |
| GET | `/api/v1/calendar/reminders/upcoming` | Предстоящие напоминания |
| GET | `/api/v1/products` | Каталог товаров |

## 🔑 Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `DATABASE_URL` | PostgreSQL URL | `postgresql+asyncpg://user:pass@localhost/db` |
| `OPENAI_API_KEY` | API ключ OpenAI | `sk-...` |
| `SECRET_KEY` | Секрет для JWT | `openssl rand -hex 32` |
| `REDIS_URL` | Redis URL | `redis://localhost:6379/0` |
| `DEBUG` | Режим отладки | `true` / `false` |

## 🔧 Makefile команды

```bash
make dev        # Запуск dev-сервера
make run        # Запуск production
make migrate    # Применить миграции
make seed       # Заполнить биомаркеры
make format     # Форматирование кода
make lint       # Проверка линтером
make docker-up  # Запуск в Docker
make docker-down # Остановка Docker
```

## 🧠 Как работает распознавание

1. **Загрузка файла** → PDF или изображение сохраняется
2. **OCR** → Tesseract извлекает текст с фото/скана
3. **AI парсинг** → GPT-4 структурирует данные в JSON:
   ```json
   {
     "biomarkers": [
       {"code": "HGB", "value": 140, "unit": "г/л", "ref_min": 130, "ref_max": 170}
     ]
   }
   ```
4. **Нормализация** → Приведение к стандартным кодам (HGB, FE, TSH...)
5. **Сравнение с нормами** → Определение статуса (норма/выше/ниже)
6. **Генерация рекомендаций** → Подбор товаров из каталога

## 📊 Поддерживаемые биомаркеры

Система распознает ~20 основных показателей:
- **Гематология**: HGB, RBC, WBC, PLT, ESR
- **Биохимия**: GLU, CHOL, ALT, AST, CREA
- **Витамины**: B12, D3, FOLATE
- **Минералы**: FE, FERR, MG, ZN, CA
- **Гормоны**: TSH, T4, T3
- **Воспаление**: CRP

Легко расширяется через `scripts/seed_biomarkers.py`.

## 📝 Лицензия

Proprietary. Все права защищены.

